% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assemble_dataset.R
\name{assemble_dataset}
\alias{assemble_dataset}
\title{Assemble a struct::DatasetExperiment from a matrix + metadata}
\usage{
assemble_dataset(x, feature_meta, sample_meta, name_col = "Name")
}
\arguments{
\item{x}{Wide numeric matrix / data frame, rows = samples, columns =
compounds (canonical `Compound` names).}

\item{feature_meta}{Feature metadata; rows with `Report == "YES"` whose
`Compound` is in `x` are kept, in matrix-column order.}

\item{sample_meta}{Sample metadata; rows whose `name_col` value is a row of
`x` are kept.}

\item{name_col}{`sample_meta` column matching the matrix row names.}
}
\value{
A `struct::DatasetExperiment`.
}
\description{
Aligns `feature_meta` and `sample_meta` to the matrix (features to columns,
samples to rows, both by name) and builds the `DatasetExperiment` so
`variable_meta`/`sample_meta` rownames match the data assay exactly.
}
\examples{
m  <- data.frame(A = c(10, 20, 30), B = c(1, 2, 3),
                 row.names = c("S1", "S2", "S3"))
fm <- data.frame(Compound = c("A", "B"), Report = "YES",
                 row.names = c("A", "B"))
sm <- data.frame(Name = c("S1", "S2", "S3"), Group = c("x", "y", "x"),
                 row.names = c("S1", "S2", "S3"))
assemble_dataset(m, fm, sm)
}
\seealso{
Other data parse:
\code{\link[=combine_datasets]{combine_datasets()}},
\code{\link[=load_config]{load_config()}},
\code{\link[=load_dataset]{load_dataset()}},
\code{\link[=read_skyline]{read_skyline()}},
\code{\link[=read_targetlynx]{read_targetlynx()}}
}
\concept{data parse}
