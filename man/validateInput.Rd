% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validateInput}
\alias{validateInput}
\title{Validate a workbook before processing it}
\usage{
validateInput(
  feature_meta,
  sample_meta,
  data = NULL,
  name_col = "Name",
  compound_col = "Compound",
  processing_name_col = "Processing_name",
  report_col = "Report",
  include_col = "Include"
)
}
\arguments{
\item{feature_meta}{Feature metadata sheet.}

\item{sample_meta}{Sample metadata sheet.}

\item{data}{Optional wide matrix (samples x compounds), e.g. from
[readTargetLynx()]. Cross-checks between data and metadata are skipped
when absent.}

\item{name_col, compound_col, processing_name_col, report_col, include_col}{Column names, matching the arguments of [processDataset()].}
}
\value{
An `mrm_validation` object with `errors`, `warnings` and a `summary`
  data frame of every check.
}
\description{
Checks the shape of the inputs - the things that make a run fail halfway
through, or worse, succeed on the wrong data. Every check runs, so one call
reports every problem rather than stopping at the first.
}
\examples{
xlsx <- system.file("extdata", "example_data.xlsx",
                    package = "MRManalyzeR")
fdata <- openxlsx::read.xlsx(xlsx, sheet = "feature_metadata")
meta  <- openxlsx::read.xlsx(xlsx, sheet = "sample_metadata")
validateInput(fdata, meta)
}
\seealso{
Other data parse:
\code{\link[=assembleDataset]{assembleDataset()}},
\code{\link[=combineDatasets]{combineDatasets()}},
\code{\link[=loadConfig]{loadConfig()}},
\code{\link[=loadDataset]{loadDataset()}},
\code{\link[=readPeakMatrix]{readPeakMatrix()}},
\code{\link[=readSkyline]{readSkyline()}},
\code{\link[=readTargetLynx]{readTargetLynx()}},
\code{\link[=validateConfig]{validateConfig()}},
\code{\link[=validateDesign]{validateDesign()}}
}
\concept{data parse}
