% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_boxplot.R
\name{plot_boxplot}
\alias{plot_boxplot}
\title{Per-compound box or bar plot by group}
\usage{
plot_boxplot(
  de,
  compound,
  group_by,
  facet_by = NULL,
  type = c("box", "bar"),
  error_bar = c("SD", "SE", "none"),
  value_label = "value",
  sample_id_head = NULL,
  tooltip_factor = NULL,
  yaxis_quant = 0.95,
  yaxis_scalar = 1.3,
  fill_brewer = TRUE
)
}
\arguments{
\item{de}{A `struct::DatasetExperiment`.}

\item{compound}{Feature (column) name to plot.}

\item{group_by}{`sample_meta` column defining the x-axis groups.}

\item{facet_by}{Optional `sample_meta` column to facet across, e.g. one
panel per comparison.}

\item{type}{`"box"` or `"bar"`.}

\item{error_bar}{`"SD"`, `"SE"` or `"none"` - bars only.}

\item{value_label}{Y-axis label, e.g. the datatype units.}

\item{sample_id_head}{`sample_meta` column identifying points in the
tooltip; `NULL` uses row names.}

\item{tooltip_factor}{Additional `sample_meta` column shown in the tooltip,
typically the primary study factor.}

\item{yaxis_quant, yaxis_scalar}{Quantile used to cap the y-axis, and the
headroom multiplier above it.}

\item{fill_brewer}{Use the package's qualitative palette for group fills;
`FALSE` keeps ggplot2's default hue scale.}
}
\value{
A `ggplot`.
}
\description{
One compound's values split by a grouping factor - the plot that actually
gets looked at when a statistical test comes back significant, because it
shows whether the difference rests on the whole group or on one animal.
}
\details{
`type = "box"` draws a box with the individual injections jittered over it,
which is the honest view for the small groups a targeted study usually has:
the reader can count the points. `type = "bar"` draws group means with SD or
SE error bars, matching [summarise_groups()] exactly. Reports commonly show
both, the box for distribution and the bar for the summary the statistics
were computed on.

The y-axis is capped at `quantile(value, yaxis_quant) * yaxis_scalar` so one
extreme injection cannot flatten the groups.
}
\examples{
de <- load_dataset(system.file("extdata", "example_synthetic.RDS",
                               package = "MRManalyzeR"))
de <- subset_dataset(de, conditions = list(Sample_type = "Sample"))
plot_boxplot(de, "Analyte_02", "Treatment", value_label = "ng/mL")
plot_boxplot(de, "Analyte_02", "Treatment", type = "bar",
             error_bar = "SE")
}
\seealso{
[summarise_groups()] for the numbers the bars are drawn from.

Other stats:
\code{\link[=plot_correlation]{plot_correlation()}},
\code{\link[=plot_heatmap]{plot_heatmap()}},
\code{\link[=plot_volcano]{plot_volcano()}},
\code{\link[=run_stats]{run_stats()}},
\code{\link[=summarise_groups]{summarise_groups()}}
}
\concept{stats}
