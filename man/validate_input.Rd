% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_input}
\alias{validate_input}
\title{Validate a workbook before processing it}
\usage{
validate_input(
  feature_meta,
  sample_meta,
  data = NULL,
  name_col = "Name",
  compound_col = "Compound",
  processing_name_col = "Processing_name",
  report_col = "Report",
  include_col = "Include"
)
}
\arguments{
\item{feature_meta}{Feature metadata sheet.}

\item{sample_meta}{Sample metadata sheet.}

\item{data}{Optional wide matrix (samples x compounds), e.g. from
[read_targetlynx()]. Cross-checks between data and metadata are skipped
when absent.}

\item{name_col, compound_col, processing_name_col, report_col, include_col}{Column names, matching the arguments of [process_dataset()].}
}
\value{
An `mrm_validation` object with `errors`, `warnings` and a `summary`
  data frame of every check.
}
\description{
Checks the shape of the inputs - the things that make a run fail halfway
through, or worse, succeed on the wrong data. Every check runs, so one call
reports every problem rather than stopping at the first.
}
\examples{
xlsx <- system.file("extdata", "example_data.xlsx",
                    package = "MRManalyzeR")
fdata <- openxlsx::read.xlsx(xlsx, sheet = "feature_metadata")
meta  <- openxlsx::read.xlsx(xlsx, sheet = "sample_metadata")
validate_input(fdata, meta)
}
\seealso{
Other data parse:
\code{\link[=assemble_dataset]{assemble_dataset()}},
\code{\link[=combine_datasets]{combine_datasets()}},
\code{\link[=load_config]{load_config()}},
\code{\link[=load_dataset]{load_dataset()}},
\code{\link[=read_peak_matrix]{read_peak_matrix()}},
\code{\link[=read_skyline]{read_skyline()}},
\code{\link[=read_targetlynx]{read_targetlynx()}},
\code{\link[=validate_config]{validate_config()}},
\code{\link[=validate_design]{validate_design()}}
}
\concept{data parse}
