% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_drift.R
\name{plot_drift}
\alias{plot_drift}
\title{Plot one compound's intensity against injection order}
\usage{
plot_drift(
  de,
  compound,
  sample_type_head = "Sample_type",
  levels = NULL,
  injection_order_head = "Injection_order",
  sample_id_head = NULL,
  value_label = "value",
  yaxis_quant = 0.95,
  yaxis_scalar = 1.3
)
}
\arguments{
\item{de}{A `struct::DatasetExperiment`.}

\item{compound}{Feature (column) name to plot.}

\item{sample_type_head}{`sample_meta` column classifying sample type.}

\item{levels}{Value(s) of `sample_type_head` to include; `NULL` keeps all.}

\item{injection_order_head}{`sample_meta` column holding acquisition order.
Falls back to row position when absent.}

\item{sample_id_head}{`sample_meta` column used to label points in the
tooltip; `NULL` uses the row names.}

\item{value_label}{Axis / tooltip label for the measured value, e.g. the
datatype units ("ng/mL", "Area").}

\item{yaxis_quant, yaxis_scalar}{Quantile used to cap the y-axis, and the
headroom multiplier applied above it.}
}
\value{
A `ggplot`.
}
\description{
The core analytical-drift check: a compound's response plotted against the
order its injections were acquired in, coloured by sample type so the pooled
QCs can be tracked through the sequence. QCs are identical material, so a
trend, a step or a widening spread in them is instrumental - loss of
sensitivity, a column change, a batch boundary - and not biology.
}
\details{
The y-axis is capped at `quantile(value, yaxis_quant) * yaxis_scalar` so a
single large value cannot flatten the rest of the series; points above the
cap are drawn outside the panel rather than dropped.

The returned plot carries a `text` aesthetic holding the per-point tooltip,
which [plotly::ggplotly()] picks up with `tooltip = "text"`; it is ignored
by a plain `print()`.
}
\examples{
de <- load_dataset(system.file("extdata", "example_synthetic.RDS",
                               package = "MRManalyzeR"))
plot_drift(de, "Analyte_02", value_label = "ng/mL")
}
\seealso{
Other QC check:
\code{\link[=add_cv_metrics]{add_cv_metrics()}},
\code{\link[=plot_loadings]{plot_loadings()}},
\code{\link[=plot_pca]{plot_pca()}},
\code{\link[=plot_qq]{plot_qq()}},
\code{\link[=run_pca]{run_pca()}},
\code{\link[=summarise_features]{summarise_features()}}
}
\concept{QC check}
