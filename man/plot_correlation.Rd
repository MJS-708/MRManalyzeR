% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_correlation.R
\name{plot_correlation}
\alias{plot_correlation}
\title{Feature-by-feature correlation heatmap}
\usage{
plot_correlation(
  correlations,
  variable_meta = NULL,
  name = NULL,
  subset = NULL,
  method = NULL,
  group_by = NULL,
  cluster = TRUE,
  show_values = FALSE
)
}
\arguments{
\item{correlations}{The `correlations` data frame from [run_stats()], or the
whole list. Long format: `feature_a`, `feature_b`, `estimate`, plus
`correlation` / `subset` / `method` identifying each run.}

\item{variable_meta}{Feature metadata, or a `DatasetExperiment` to take it
from. Only needed for `group_by`.}

\item{name, subset, method}{Select one run when the table holds several.
`NULL` requires that only one is present.}

\item{group_by}{`variable_meta` column used to block and colour features.}

\item{cluster}{Cluster features within each block (globally when there is no
`group_by`), using average linkage on `1 - |r|`.}

\item{show_values}{Print the coefficient in each cell. Ignored above 30
features, where the text is unreadable anyway.}
}
\value{
A `ggplot`, or `NULL` if the selection is empty.
}
\description{
Draws one triangle of the correlation matrix for a set of compounds. Where
the comparisons ask whether a compound differs between groups, this asks
which compounds move *together* - which in a targeted panel usually means
they share a branch of the pathway, so blocks of correlation along the
diagonal are the pathway structure showing itself.
}
\details{
Only one triangle is drawn, because a correlation matrix is symmetric and
showing both halves doubles the ink for no information. The diagonal is left
grey unless self-correlations are present in the input.

Features are ordered by their `group_by` class into contiguous blocks and
clustered within each block, so a class stays together and reads as a unit;
with no `group_by`, clustering is global. A coloured strip along each axis
carries the class, using the same palette as [plot_loadings()] and the
feature annotation in [plot_heatmap()] - so a class is one colour everywhere
in the report.
}
\examples{
de <- load_dataset(system.file("extdata", "example_synthetic.RDS",
                               package = "MRManalyzeR"))
de <- subset_dataset(de, conditions = list(Sample_type = "Sample"))
params <- list(correlations = list(enabled = TRUE, entries = list(
  list(name = "pairs", methods = "spearman", subsets = list(list())))))
st <- run_stats(de, params)
plot_correlation(st, de, group_by = "Enzymatic_pathway")
}
\seealso{
Other stats:
\code{\link[=plot_boxplot]{plot_boxplot()}},
\code{\link[=plot_group_heatmap]{plot_group_heatmap()}},
\code{\link[=plot_heatmap]{plot_heatmap()}},
\code{\link[=plot_volcano]{plot_volcano()}},
\code{\link[=run_stats]{run_stats()}},
\code{\link[=summarise_groups]{summarise_groups()}}
}
\concept{stats}
