% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generateDataMatrix.R
\name{generateDataMatrix}
\alias{generateDataMatrix}
\title{Build a sample x compound matrix from a stacked TargetLynx long table}
\usage{
generateDataMatrix(
  fdata,
  metadata,
  lcms_table,
  datatype = "Area",
  tl_headers = c("ID", "Name", "Area", "ng/mL", "Response", "S/N"),
  use_type = "Ratio",
  snr = 5,
  blank_filter = 5,
  replace_MVs = FALSE,
  batch_correction = FALSE,
  bc_qc_label = "Sample",
  bc_factor_name = "Sample_type",
  bc_header = "extract_batch",
  blank_head = "Sample_type1",
  blank_name = "ExtractBlank",
  normalize = FALSE,
  adjust_conc = FALSE
)
}
\arguments{
\item{fdata}{Feature metadata. Must contain columns `Processing_name`,
`Compound`, `Report`, `Comment`.}

\item{metadata}{Sample metadata. Must contain `Name`, `Include` and the
headers referenced by `bc_header`, `bc_factor_name`, `blank_head`.}

\item{lcms_table}{Long table from [`extractTable()`].}

\item{datatype}{TargetLynx column to report (e.g. "Area", "Response", "ng/mL").}

\item{tl_headers}{Retained for API compatibility; not used internally.}

\item{use_type}{Retained for API compatibility.}

\item{snr}{S/N threshold. Peaks with S/N below this are set to NA. `FALSE` to skip.}

\item{blank_filter}{Blank-filter factor; `FALSE` to skip.}

\item{replace_MVs}{Scalar passed to [`replaceNA()`]; `FALSE` to skip.}

\item{batch_correction}{Logical.}

\item{bc_qc_label, bc_factor_name, bc_header}{Batch-correction parameters.}

\item{blank_head, blank_name}{Metadata column and value identifying blanks.}

\item{normalize}{Metadata column used as divisor, or `FALSE`.}

\item{adjust_conc}{Logical. If `TRUE`, volume-adjust via [`calculate_conc()`].}
}
\value{
A list: `[[1]]` the `DatasetExperiment`, `[[2]]` a data frame of
  removed features.
}
\description{
Applies (optionally) SNR filtering, blank filtering, missing-value
imputation, normalisation, concentration adjustment and batch correction
to produce a `struct::DatasetExperiment`.
}
\details{
Top-level orchestrator. Heavy lifting is delegated to the internal
helpers `.build_wide_matrix()`, `.apply_snr_mask()`, `.process_batch()`
and `.assemble_DE()`.
}
