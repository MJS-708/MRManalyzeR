% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de_subset.R
\name{de_subset}
\alias{de_subset}
\title{Subset a `DatasetExperiment` by sample-metadata conditions}
\usage{
de_subset(de, conditions = NULL, features = NULL, drop_empty_features = TRUE)
}
\arguments{
\item{de}{A `struct::DatasetExperiment`.}

\item{conditions}{A named list, e.g. `list(Treatment = "HDM", Sex = "Male")`.
Values may be a scalar or vector (interpreted as `%in%`). `NULL` or
empty list returns the input unchanged on the sample dimension.}

\item{features}{Optional character vector of feature (column) names to
retain. `NULL` keeps all features.}

\item{drop_empty_features}{If `TRUE` (default), features that are entirely
`NA` after subsetting are dropped from both `data` and `variable_meta`.}
}
\value{
A new `DatasetExperiment` with `data`, `sample_meta`, and
  `variable_meta` consistently sliced.
}
\description{
Filters `sample_meta` to rows matching all `conditions` (a named list of
`column = value` pairs, joined by AND) and slices `data` to the matching
samples. Optionally restricts to a feature subset.
}
