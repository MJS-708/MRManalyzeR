% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_skyline.R
\name{readSkyline}
\alias{readSkyline}
\title{Read Skyline sheet(s) into a wide sample x compound matrix}
\usage{
readSkyline(
  xlsx_path,
  data_tab_names = "skyline_data",
  feature_meta,
  signal_filter = FALSE
)
}
\arguments{
\item{xlsx_path}{Path to the Skyline xlsx workbook.}

\item{data_tab_names}{Sheet name(s) holding the molecule x sample matrix.}

\item{feature_meta}{Feature metadata (canonical `Processing_name` / `Report`
columns; plus the `LOD`/`LOQ` column when `signal_filter` is set).}

\item{signal_filter}{`"LOD"`, `"LOQ"`, or `FALSE` - the per-compound floor
below which values are set to `NA`.}
}
\value{
A data frame, rows = samples, columns = compounds, numeric.
}
\description{
Reads the Skyline "molecule x sample" sheet(s), renaming molecule columns to
the canonical `Processing_name` where matched in `feature_meta`, and
optionally masks values below the per-compound `LOD`/`LOQ` threshold.
}
\examples{
sky <- data.frame(Molecule = c("PGE2", "PGD2"),
                  S1 = c(100, 50), S2 = c(120, 55))
f <- tempfile(fileext = ".xlsx")
openxlsx::write.xlsx(list(skyline_data = sky), f)
fm <- data.frame(Processing_name = c("PGE2", "PGD2"), Report = "YES",
                 row.names = c("PGE2", "PGD2"))
readSkyline(f, "skyline_data", fm)
}
\seealso{
Other data parse:
\code{\link[=assembleDataset]{assembleDataset()}},
\code{\link[=combineDatasets]{combineDatasets()}},
\code{\link[=loadConfig]{loadConfig()}},
\code{\link[=loadDataset]{loadDataset()}},
\code{\link[=readPeakMatrix]{readPeakMatrix()}},
\code{\link[=readTargetLynx]{readTargetLynx()}},
\code{\link[=validateConfig]{validateConfig()}},
\code{\link[=validateDesign]{validateDesign()}},
\code{\link[=validateInput]{validateInput()}}
}
\concept{data parse}
