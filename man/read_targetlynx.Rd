% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_targetlynx.R
\name{read_targetlynx}
\alias{read_targetlynx}
\title{Read a TargetLynx workbook into a wide sample x compound matrix}
\usage{
read_targetlynx(
  xlsx_path,
  datatype = "Area",
  tl_headers = c("ID", "Name", "Area", "ng/mL", "Response", "S/N"),
  snr = FALSE,
  data_tab_names = NULL
)
}
\arguments{
\item{xlsx_path}{Path to the TargetLynx xlsx workbook.}

\item{datatype}{TargetLynx column to report (e.g. "Area", "Response",
"ng/mL").}

\item{tl_headers}{TargetLynx column headers to extract.}

\item{snr}{S/N threshold; values below it are set to `NA`. `FALSE` to skip.}

\item{data_tab_names}{Sheet name(s) to read; `NULL` auto-matches sheets
whose name contains "lcms_data".}
}
\value{
A data frame, rows = samples, columns = compounds
  (`Processing_name`), numeric.
}
\description{
Reads the TargetLynx sheet(s) with [extractTable()], pivots the long table
to a wide samples x compound matrix of the requested `datatype`, and
optionally masks values whose per-injection S/N is below `snr`. Columns are
the raw TargetLynx compound (`Processing_name`) identifiers; the
rename-to-`Compound` and feature/sample filtering happen later in
[process_dataset()].
}
\examples{
xlsx <- system.file("extdata", "example_data.xlsx", package = "MRManalyzeR")
m <- read_targetlynx(xlsx, datatype = "Area", snr = 3)
dim(m)
}
\seealso{
Other workflow steps:
\code{\link[=adjust_concentration]{adjust_concentration()}},
\code{\link[=assemble_dataset]{assemble_dataset()}},
\code{\link[=correct_batch]{correct_batch()}},
\code{\link[=filter_blanks]{filter_blanks()}},
\code{\link[=impute_missing]{impute_missing()}},
\code{\link[=normalise_matrix]{normalise_matrix()}},
\code{\link[=process_dataset]{process_dataset()}},
\code{\link[=read_skyline]{read_skyline()}}
}
\concept{workflow steps}
